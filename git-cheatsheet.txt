
git init
git commit -m 'inital commit'

git clone https://github.com/uuxy/testing

git add file/folder_name

git commit -m 'folder added'

(in case of next block doesn't work -- git add -A  and then do the following step)

git rm -rf folder_name
git commit -m 'folder removed'

git push origina master


git config --global user.name 'uuxy'

git config --global user.email 'xy20150828@gmail.com'

 